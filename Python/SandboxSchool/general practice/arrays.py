
new_list = [1, 2, 3]

result = new_list[0]

# access on an list in python is O(1).  constant time
# its because lists are stored in contiguous memory so it is easy for the cpu to determine the location of any element based off the 0th index.


numbers = [5, 1, 3, 2, 6]


nums = []

